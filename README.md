# Todo Reminder

一个使用 Next.js App Router 构建的待办提醒应用。

## 功能

- 新增待办任务、备注、提醒时间和优先级
- 使用 `localStorage` 在浏览器本地保存任务
- 筛选全部、进行中、已过期和已完成任务
- 完成、删除和延后 10 分钟
- 页面打开时自动检查到期任务并显示提醒

## 开发命令

```bash
npm.cmd run dev
npm.cmd run build
npm.cmd run start
npm.cmd run lint
```

开发服务器默认运行在 `http://localhost:3000`。
